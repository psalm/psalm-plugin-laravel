<?xml version="1.0"?>
<!-- Explicit opt-out configuration for the D-in TaintedLlmPrompt issue. -->
<psalm
    disableSuppressAll="true"
    ensureArrayStringOffsetsExist="true"
    errorLevel="1"
    findUnusedBaselineEntry="true"
    findUnusedPsalmSuppress="true"
    findUnusedVariablesAndParams="true"
    memoizeMethodCallResults="true"
    reportMixedIssues="true"
    runTaintAnalysis="false"
    sealAllMethods="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config ../../vendor/vimeo/psalm/config.xsd"
>
    <plugins>
        <pluginClass class="Psalm\LaravelPlugin\Plugin">
            <failOnInternalError value="true" />
            <resolveDynamicWhereClauses value="true" />
            <findPromptInjection value="false" />
        </pluginClass>
    </plugins>
    <issueHandlers>
        <!-- MissingPureAnnotation does not exist on Psalm 6 (Psalm 7 core issue, absent
             from config.xsd's issueHandlers), so there is nothing to downgrade here. -->
        <PluginIssue name="NoEnvOutsideConfig" errorLevel="info" />
    </issueHandlers>
</psalm>
