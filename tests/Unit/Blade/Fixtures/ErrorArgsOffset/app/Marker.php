<?php

declare(strict_types=1);

namespace Fx;

// Unused: this fixture only exercises `@error` bookkeeping and a plain `@php` array offset, neither
// of which needs a real class — kept only so `<projectFiles><directory name="app" /></projectFiles>`
// has something to scan.
final class Marker {}
